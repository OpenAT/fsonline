# -*- coding: utf-8 -*-

from odoo import fields, models


class {{ class_name }}(models.Model):
    name = "{{ model_name }}"
    {%- if inherit_from %}
    _inherit = "{{ inherit_from }}"{% endif %}
    _description = "{{ description }}"
